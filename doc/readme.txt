hr-salary-admin/
├── public/                         # 静态资源（favicon、静态 HTML）
│   └── favicon.ico
│
├── src/                            # 项目源码主目录
│   ├── assets/                     # 资源文件（图片、图标、字体等）
│   │   ├── images/
│   │   ├── icons/
│   │   └── fonts/
│
│   ├── components/                 # 通用组件库
│   │   ├── common/                # 基础组件（Icon、Button、Loading 等）
│   │   ├── layout/                # 布局组件（Sidebar、Header、Breadcrumb）
│   │   ├── reusable/              # 高复用组件（Table、Form、Chart 等）
│   │   └── business/              # 业务组件（特定业务场景封装）
│
│   ├── views/                      # 页面视图（按路由划分）
│   │   ├── login/                 # 登录模块
│   │   ├── dashboard/             # 仪表盘
│   │   ├── system/                # 系统管理（用户、角色）
│   │   ├── salary/                # 薪资模块（部门、员工、用户）
│   │   └── error/                 # 异常页面（403、404）
│
│   ├── router/                     # 路由配置
│   │   ├── index.ts               # 路由入口
│   │   ├── guard.ts               # 路由守卫
│   │   └── modules/               # 路由模块化配置
│
│   ├── stores/                     # 状态管理（Pinia）
│   │   ├── modules/               # 模块化 Store（user、app、tabs、theme）
│   │   └── index.ts               # Store 统一导出
│
│   ├── api/                        # API 接口层
│   │   ├── modules/               # 模块化接口（system、employee、attendance）
│   │   ├── index.ts               # API 统一导出
│   │   └── request.ts             # Axios 配置与拦截器
│
│   ├── composables/               # Vue 3 组合式函数（Hooks）
│   │   ├── core/                  # 通用逻辑（useTable、useForm、usePagination）
│   │   ├── business/              # 业务逻辑（useEmployee、useAttendance）
│   │   └── index.ts               # Hooks 统一导出
│
│   ├── types/                      # TypeScript 类型定义
│   │   ├── api/                   # API 类型
│   │   ├── store/                 # Store 类型
│   │   ├── components/            # 组件 Props 类型
│   │   └── global.d.ts            # 全局类型声明
│
    styles/                             # 🌈 全局样式系统主目录
    ├── index.scss                      # 📌 样式主入口，统一导入所有模块
    ├── _reset.scss                    # 🔄 CSS 重置样式（清除浏览器默认样式）
    ├── _common.scss                   # 🧱 通用基础样式（布局、按钮、表单等）
    ├── _utilities.scss                # 🛠️ 工具类样式（边距、字体大小、文本对齐等）
    ├── _animations.scss               # 🎞️ 动画效果（过渡、淡入淡出等）
    ├── _mixins.scss                   # 🧩 Sass 混入（响应式、文本截断、flex 等）
    ├── _breakpoints.scss              # 📱 响应式断点配置（统一媒体查询断点）
    │
    ├── variables/                     # 🎨 样式变量模块（颜色、字体、间距等）
    │   ├── _colors.scss               # 🎨 颜色变量（主色、背景色、文本色等）
    │   ├── _fonts.scss                # 🔤 字体变量（字体族、字号、行高等）
    │   ├── _spacing.scss              # 📏 间距变量（padding、margin 等）
    │   ├── _zindex.scss               # 🧭 层级变量（z-index 管理）
    │   └── _index.scss                # 📦 变量统一导入入口
    │
    ├── components/                    # 🧩 组件级样式（按组件拆分）
    │   ├── _button.scss               # 🔘 按钮组件样式
    │   ├── _card.scss                 # 🧾 卡片组件样式
    │   ├── _table.scss                # 📊 表格组件样式
    │   └── _form.scss                 # 📝 表单组件样式
    │
    └── theme/                         # 🎭 主题系统（支持多套主题）
        ├── _light.scss                # 🌞 明亮主题变量（默认）
        ├── _dark.scss                 # 🌚 暗黑主题变量
        ├── _custom.scss               # 🎨 用户自定义主题变量（运行时注入）
        ├── _blue.scss                 # 🔵 蓝色主题预设（可选）
        ├── _green.scss                # 🟢 绿色主题预设（可选）
        └── _index.scss                # 📦 主题统一导入入口

│
│   ├── utils/                      # 工具函数（格式化、校验等）
│   ├── constants/                 # 常量（枚举、配置项）
│   ├── permissions/              # 权限控制逻辑
│   ├── locales/                  # 国际化资源（i18n）
│   ├── App.vue                   # 根组件
│   ├── main.ts                   # 应用入口
│   └── env.d.ts                  # Vite 环境类型声明
│
├── tests/                         # 测试目录（单元测试、集成测试）
│   ├── unit/
│   └── e2e/
│
├── .env                           # 默认环境变量
├── .env.development              # 开发环境变量
├── .env.production               # 生产环境变量
├── index.html                    # Vite HTML 入口
├── tsconfig.json                 # TypeScript 配置
├── vite.config.ts                # Vite 配置
├── package.json                  # 项目依赖与脚本
├── README.md                     # 项目说明文档
└── pnpm-lock.yaml / yarn.lock    # 包管理锁文件
==========================================================================================================================================
Day 1：项目初始化与基础环境
初始化项目（Vite + Vue3 + TypeScript + Pinia + Vue Router）
配置 ESLint + Prettier + Husky（保证代码质量）
建立基础目录结构（你提供的结构）
配置 vite.config.ts 支持别名 @ 指向 src
在 App.vue 中写一个简单的 Hello World 页面，验证环境是否正常

Day 2：路由系统 & 登录页面
配置 router/index.ts，实现基本路由跳转
添加路由守卫 router/guard.ts（未登录跳转到 login）
编写 views/login/ 页面（账号密码输入 + 登录按钮）
使用 Pinia 建立 stores/modules/user.ts，存储用户信息和 token
登录成功后跳转到 dashboard

Day 3：布局系统 & 全局样式
编写 components/layout/Sidebar.vue、Header.vue、Breadcrumb.vue
在 App.vue 中引入布局，形成基础框架
配置 styles/index.scss，导入 reset、common、utilities
验证布局是否能正常切换页面


Day 4：主题系统（暗黑模式 + 自定义主题色）
在 styles/theme/ 中编写 _light.scss、_dark.scss
在 stores/modules/theme.ts 中存储当前主题
在 App.vue 中通过 class="theme-dark" 或 class="theme-light" 切换主题
实现用户自定义主题色（修改 variables/_colors.scss，通过 CSS 变量注入）
在 Header 添加一个主题切换按钮 + 颜色选择器

Day 5：系统管理 - 用户管理
在 views/system/user/ 编写用户管理页面
使用 components/reusable/Table.vue 展示用户列表
使用 components/reusable/Form.vue 实现新增/编辑用户
在 api/modules/system.ts 中编写用户相关接口（增删改查）
调试接口，确保用户管理功能可用

Day 6：系统管理 - 角色管理
在 views/system/role/ 编写角色管理页面
使用表格展示角色列表
使用表单新增/编辑角色
在 api/modules/system.ts 中编写角色相关接口
在用户管理中关联角色（用户分配角色

Day 7：系统管理 - 菜单管理
在 views/system/menu/ 编写菜单管理页面
使用树形表格展示菜单结构
使用表单新增/编辑菜单
在 permissions/ 中编写权限控制逻辑（根据菜单和角色动态生成路由）
验证不同角色登录后，菜单和路由是否正确显示


Day 8：优化与测试
在 tests/unit/ 编写单元测试（例如：用户 store、主题切换）
在 tests/e2e/ 编写端到端测试（登录流程、菜单权限）
优化代码结构，抽离公共逻辑到 composables/core/
编写 README.md，记录搭建过程和使用说明


==========================================================================================================================================



技术栈
Vue 3 + Composition API
TypeScript
Vite
Pinia
Sass
Vue Router
Axios
ESLint + Prettier

一、初始化项目
npm create vite@latest hr-salary-admin -- --template vue-ts
cd salary-admin
npm install

二、安装核心依赖
npm install vue-router@4 pinia sass axios

==============================